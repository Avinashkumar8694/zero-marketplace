# zero-navbar

Responsive top navigation bar. Brand/logo text, JSON-configured links, alignment, four variants (solid / glass / transparent / elevated), optional sticky, and an optional CTA button. Emits `on-link-click` with the clicked link target.
