# zero-pagination

Page navigation control. Renders previous / numbered pages / next, with optional first & last jumps (`showFirstLast`) and smart ellipsis truncation driven by `siblingCount`. Three variants (default / rounded / minimal), the active page highlighted with the primary token. Emits `on-page-change` with the new page number. Themed from `--uiv-*` tokens.
