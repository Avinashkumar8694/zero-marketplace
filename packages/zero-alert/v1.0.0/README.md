# zero-alert

Inline notification / feedback banner. Title + message, four semantic types (info / success / warning / error) themed from `--uiv-color-*` tokens, three variants (soft / solid / outline), optional icon and optional dismiss button. Dismissing hides the alert and emits `on-dismiss`.
