# zero-badge

Compact status / label badge. Text label, five semantic types (neutral / primary / success / warning / error) themed from theme tokens, four variants (soft / solid / outline / dot), three sizes and an optional rounded pill shape. Emits `on-click` when tapped.
