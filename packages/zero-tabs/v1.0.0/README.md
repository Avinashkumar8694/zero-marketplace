# zero-tabs

Tabbed content switcher. JSON-configured tabs ({label, value}), controllable active value, three variants (underline / pills / enclosed) and optional full-width stretch. Clicking a tab updates the active tab and emits `on-tab-change` with the selected value. Panel content is slotted.
