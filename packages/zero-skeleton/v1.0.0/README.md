# zero-skeleton

Loading placeholder. Four variants (text / circle / rect / card), custom width and height strings (e.g. `100%`, `200px`), a configurable line count for the text variant, three animation modes (pulse / wave / none) and an optional rounded flag. Colors come from the theme background, border and surface tokens. Emits `on-click` (nominal).
