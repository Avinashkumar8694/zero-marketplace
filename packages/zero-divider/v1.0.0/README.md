# zero-divider

Separator rule. Horizontal or vertical orientation, an optional centered label (horizontal only), three line styles (solid / dashed / dotted), configurable thickness and spacing in pixels, and a custom color that falls back to the theme border token. The label uses the muted text token. Emits `on-click`.
