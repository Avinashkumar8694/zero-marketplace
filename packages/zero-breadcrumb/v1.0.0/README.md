# zero-breadcrumb

Breadcrumb trail navigation. JSON-configured items ({label, href}), a customizable separator (default `/`), and three variants (default / chips / underline). The last item is marked as the current page; clicking any other crumb emits `on-item-click` with its label, href and index.
